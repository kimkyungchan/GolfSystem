<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="golf.project.dao.ResSelDao">
  
  <select id = "selectRes" parameterType="String" resultType="golf.project.dto.ResSelDto">
    select * from admin_res_sel where seldate = #{seldate}
    order by selcourse, seltime
    
  
  </select>
  
  
  
  </mapper>